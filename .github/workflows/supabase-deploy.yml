name: Deploy Supabase Changes

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}

    steps:
      - uses: actions/checkout@v3

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Supabase Login
        run: supabase login $SUPABASE_ACCESS_TOKEN

      - name: Backup and Migrate Supabase DB
        working-directory: ./packages/supacreator/supabase
        run: |
          supabase link --project-ref $PROJECT_ID
          DUMP_FILE="db_dump_$(date +%Y%m%d_%H%M%S).sql"
          supabase db dump -f $DUMP_FILE
          echo "DUMP_FILE=$DUMP_FILE" >> $GITHUB_ENV
          ls -la
          pwd

      - name: Prepare for Upload
        run: |
          DUMP_FILE_PATH=$(find ${{ github.workspace }} -name "${{ env.DUMP_FILE }}")
          echo "Found dump file at: $DUMP_FILE_PATH"
          echo "DUMP_FILE_PATH=$DUMP_FILE_PATH" >> $GITHUB_ENV

      - name: Upload DB Dump to DO Spaces
        uses: BetaHuhn/do-spaces-action@v2
        with:
          access_key: ${{ secrets.DO_ACCESS_KEY }}
          secret_key: ${{ secrets.DO_SECRET_KEY }}
          space_name: ${{ secrets.DO_SPACE_NAME }}
          space_region: ${{ secrets.DO_SPACE_REGION }}
          source: ${{ env.DUMP_FILE_PATH }}
          out_dir: supabase_backups

      - name: Deploy Supabase Migration
        working-directory: ./packages/supacreator/supabase
        run: supabase db push

      - name: Clean up local dump file
        run: |
          if [ -f "${{ env.DUMP_FILE_PATH }}" ]; then
            rm "${{ env.DUMP_FILE_PATH }}"
            echo "Deleted dump file: ${{ env.DUMP_FILE_PATH }}"
          else
            echo "Dump file not found at: ${{ env.DUMP_FILE_PATH }}"
          fi
